AskMe - A project where you can chat with the bot to and fro

How to use?
1. Pull the code from the repo.
2. Make sure you have python, python-dotenv, openai installed
   `pip install python-dotenv openai`
3. Generate the OPEN_API_KEY and add it in .env
4. run the example.py

